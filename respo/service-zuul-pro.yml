zuul:
  routes:
    first:
      path: /api-r/**
      serviceId: service-ribbon
    second:
      path: /api-f/**
      serviceId: service-feign
    sixth:
      path: /api-e/**
      serviceId: service-feign
ribbon:
  eureka:
    enabled: false

service-feign:
  ribbon:
    listOfServers: http://localhost:7002/,http://localhost:7003/
